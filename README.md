# ByteForge

An interactive prototype for an AI-powered criminal network analysis workspace. The demo contains an investigation dashboard, knowledge graph, evidence-linked cross-case insights, location clusters, and a simulated case-ingestion flow.

The site is deployed through GitHub Pages whenever changes are pushed to `main`.
